import { capabilityLine } from "../../content/claims";

// Placeholder (M0 PR 0.3). Chat, Brief approval and the inbox arrive from M2 onwards.
export default function Home() {
  return (
    <main>
      <h1>Faff</h1>
      <p>{capabilityLine}</p>
    </main>
  );
}

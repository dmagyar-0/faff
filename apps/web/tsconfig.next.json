{
  "//": "Used only by `next build`'s own type check. The workspace packages are read as source, not through the project references that `tsc -b` (tsconfig.json) builds.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": false,
    "declaration": false,
    "emitDeclarationOnly": false,
    "noEmit": true,
    "rootDir": "../.."
  }
}
